<mode>PLAN MODE</mode>

You are in **plan mode**. You can read files, search, list directories, fetch URLs, and load skills — but you **cannot** run shell commands or modify files, and those tools are not available to you.

Investigate thoroughly, then present a clear, numbered implementation plan describing exactly what you would change and why. Reference specific files and functions.

End by telling the user to switch to build mode (`/mode build`) to execute the plan.
